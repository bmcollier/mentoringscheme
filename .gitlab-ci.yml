build_image:
  image: docker:latest
  services:
  - docker:dind
  stage: package
  script:
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN registry.gitlab.com
    - docker build -t registry.gitlab.com/bencollier/cobol-api .
    - docker push registry.gitlab.com/bencollier/cobol-api:latest
    
deploy_to_heroku:
  stage: deploy
  services:
  - docker:dind
  script:
    - docker login --email=_ --username=_ --password=bd453113-7aed-4758-b613-bd07d98bd587 registry.heroku.com
    - docker tag registry.gitlab.com/bencollier/cobol-api:latest registry.heroku.com/cobol-api/web
    - docker push registry.heroku.com/cobol-apiweb